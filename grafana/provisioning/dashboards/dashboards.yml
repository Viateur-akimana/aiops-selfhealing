apiVersion: 1

providers:
  - name: TechStream
    orgId: 1
    folder: TechStream
    folderUid: techstream
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
